import React from "react";

const CancelReservationModal = () => {
  return <div>CancelReservationModal</div>;
};

export default CancelReservationModal;

export const revalidate = 60; // Revalidate every 60 seconds
